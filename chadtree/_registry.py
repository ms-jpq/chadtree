from .transitions import (
    autocmds,
    click,
    collapse,
    copy_name,
    cut_copy,
    delete,
    filter,
    focus,
    help,
    link,
    marks,
    new,
    noop,
    open_system,
    quit,
    refresh,
    rename,
    resize,
    schedule_update,
    selection,
    stat,
    toggle_exec,
    toggle_open,
    toggles,
    version_ctl,
)

assert autocmds
assert click
assert collapse
assert copy_name
assert cut_copy
assert delete
assert filter
assert focus
assert help
assert link
assert marks
assert new
assert noop
assert open_system
assert quit
assert refresh
assert refresh
assert rename
assert resize
assert schedule_update
assert selection
assert stat
assert toggle_exec
assert toggle_open
assert toggles
assert version_ctl

____ = None
